'use strict';
require('./main');
var fs = require('fs');

describe('Trigger alert', function () {
  it('should send an alert if more than 20  requests are sent in 2 minutes', function(done) {
    done();
  });
});
